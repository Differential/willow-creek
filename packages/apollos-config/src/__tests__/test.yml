ROCK:
  # Comment here
  API_URI: 'https://apollosrock.newspring.cc/api'
  API_TOKEN: ${ROCK_TOKEN}
  NULL_VALUE:
CONTENT_IDS:
  - 1
  - 5
  - ${CONTENT_ID_FROM_ENV}
COMPOUND_ENV_VALUE: "${SOME_VALUE} and then ${ANOTHER_VALUE}"
